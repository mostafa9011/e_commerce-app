import 'package:e_commerce_app/main.dart';
import 'package:flutter/material.dart';

class Constant {
  static final theme = Theme.of(navigatorKey.currentState!.context);
  static final mediaQuery = MediaQuery.of(navigatorKey.currentState!.context);
}
